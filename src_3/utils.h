#ifndef UTILS
#define UTILS

#include <string>
#include <iostream>
#include <vector>
#include <chrono>
#include <utility>
#include <map>
#include <cmath>
#include <fstream>
#include <sstream> 
#include <algorithm> // replace
#include <random>

using namespace std;
using namespace chrono;

typedef std::vector<double> point_values;
typedef std::pair<int, point_values> point;






#endif